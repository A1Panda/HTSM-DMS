# 检查清单 (Checklist)

- [x] 后端正确预处理了 `0/1` 语法，将其替换为 `*/1`。
- [x] 后端保存配置接口 (`updateConfig`) 增加了 Cron 表达式校验，无效时返回 400 状态码。
- [x] 提交 `0 0/1 * * * ?` 表达式能够成功保存并在后端正确启动调度任务。
- [x] 提交明显无效的表达式（如 `abc`）时，前端能正确弹出后端返回的错误提示（例如“无效的 Cron 表达式，请检查格式”）。
