- [x] 后端支持按 `codeStart` 和 `codeEnd` 筛选编码范围
- [x] 后端支持按 `startDate` 和 `endDate` 筛选日期范围
- [x] 后端支持按 `keyword` 模糊搜索编码描述
- [x] 前端包含独立的高级搜索页面（路由为 `/search`）
- [x] 高级搜索表单能够正确发起多条件组合请求并渲染结果（包含分页）
- [x] 能够将最近的搜索条件保存为历史记录并在页面上展示
- [x] 能够手动保存和命名当前搜索条件，并在页面上展示
- [x] 导航栏（Header）中包含"高级搜索"的入口链接
- [x] README.md 文件中对应的代办项已更新为完成状态
